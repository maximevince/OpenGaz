# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ---- OpenGaz: NEVER commit original LavaMind assets (dev-only local pack) ----
public/original/
assets-original/
*.iso
# local env
.env
.env.*

# local research dumps and working notes — not part of the repo
docs/reference/
docs/DESIGN.md

# local tooling scratch space (agent worktrees, session state)
.claude/

# music build cache (third-party source recordings, never committed)
.cache/
build/
